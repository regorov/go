package main

/*
import (
    "bufio"
    "github.com/kierdavis/go/neunet2"
    "github.com/kierdavis/go/resourcemanager"
    "os"
)

var rm = resourcemanager.NewResourceManager("github.com/kierdavis/go/neunet2/neunet2test")

func die(err error) {
    if err != nil {
        panic(err)
    }
}

func loadDataFile(filename string) (set *neunet2.Set) {
    f, err := os.Open(filename)
    die(err)
    defer f.Close()

    reader := bufio.NewReader(f)

    for {
        input1str, err := reader.ReadString('\t')
        die(err)
        input2str, err := reader.ReadString('\t')
        die(err)
        input3str, err := reader.ReadString('\t')
        die(err)
        input4str, err := reader.ReadString('\t')
        die(err)
        output1str, err := reader.ReadString('\t')
        die(err)
        output2str, err := reader.ReadString('\t')
        die(err)
        output3str, err := reader.ReadString('\n')
        die(err)
    }
}
*/

func main() {

}
